# Evidence

Committed evidence from discovery and replay runs: step records, run manifests, and
screenshots.

This directory is deliberately not ignored by git: example run evidence is a
deliverable of the assignment. It is empty until evidence capture lands in a later
phase. Override the location with `EVIDENCE_DIR`.

Anything committed here must be reviewed first. No credentials, no tokens, no
personal data, no machine-specific absolute paths.
