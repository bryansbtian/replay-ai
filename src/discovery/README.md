# discovery

The exploration loop: takes a natural-language goal, drives a surface through the
`llm` layer, and records what worked so it can be promoted into a capability artifact.

Depends on: `llm`, `execution`, `artifacts`, `policy`, `evidence`.

Empty in Phase 1.
