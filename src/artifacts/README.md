# artifacts

Typed, versioned capability artifacts: the schema, parsing, and on-disk persistence.

Depends on: `execution` (action vocabulary), `config`.
Consumed by: `discovery` (writes) and `replay` (reads).

Empty in Phase 1. The artifact schema lands in Phase 2.
