# evidence

Structured evidence capture for discovery and replay runs: step records, screenshots,
and run manifests written under `EVIDENCE_DIR`.

Depends on: `config`, `logging`. Depends on nothing above it.

Empty in Phase 1.
