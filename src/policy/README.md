# policy

Safety guardrails evaluated before an action is executed: allowed surfaces, blocked
action classes, and the checks that force a handoff instead of an action.

Depends on: `config`. Depends on nothing above it.

Empty in Phase 1.
