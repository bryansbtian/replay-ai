# surfaces

Adapters over the things being automated. The first is a Playwright-backed browser
surface; the interface is written so other surfaces can follow without touching
`execution`.

Depends on: `config`, `logging`.

Empty in Phase 1.
