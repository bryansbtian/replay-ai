/**
 * The Ollama implementation of `LLMClient`. Imported by a composition root and by nothing
 * else: everything above the boundary depends on `../index.js`.
 */
export { OllamaClient, type FetchLike, type OllamaClientOptions } from './OllamaClient.js';
