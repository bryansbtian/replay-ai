/**
 * The Anthropic implementation of `LLMClient`. Imported by a composition root and by
 * nothing else: everything above the boundary depends on `../index.js`.
 */
export {
  AnthropicClient,
  toModelError,
  type AnthropicClientOptions,
} from './AnthropicClient.js';
