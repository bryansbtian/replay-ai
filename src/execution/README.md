# execution

The action vocabulary and the executor that applies one action to one surface.
This is the shared waist: both discovery and replay drive the UI through it, which
is what makes a discovered run replayable.

Depends on: `surfaces`, `policy`, `evidence`.

Empty in Phase 1.
